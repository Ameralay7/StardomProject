using UnityEngine;
using System.Collections;

public class Button_OpenMarket : MonoBehaviour {

	//public GameObject CurrentScreen;

	void Start () {
	
	}
	

    /*
	void OnClick () {
        
		ScreensManager.Instance.MarketScreen.SetActive (true);
		ScreensManager.Instance.lastOpenedScreen.SetActive (false);
		ScreensManager.Instance.lastOpenedScreen = ScreensManager.Instance.MarketScreen;
		//CurrentScreen.SetActive (false);
		//MarketOfUsersManager.Instance.DisplayMarketWithData ();
		MarketOfUsersManager.Instance.InitMarketWithUsers();


		ScreensManager.Instance.lastActivatedTopWhiteBarButton.GetComponentInChildren<TweenAlpha> ().Toggle ();
		ScreensManager.Instance.lastActivatedTopWhiteBarButton = gameObject;
		GetComponentInChildren<TweenAlpha> ().Toggle ();
	}
     * */
}
